name = "voyage-email-receiver"
main = "src/index.ts"
compatibility_date = "2024-01-01"

# R2 bucket binding
[[r2_buckets]]
binding = "VOYAGE_DOCUMENTS"
bucket_name = "voyage-documents"

# Environment variables (secrets set via wrangler secret)
[vars]
API_URL = "https://apps.voyage.xyz"
